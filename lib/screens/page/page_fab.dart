import 'package:flutter/material.dart';

class PageFab extends StatelessWidget {
  const PageFab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page Fab'),
      ),
      body: Container(),
    );
  }
}
